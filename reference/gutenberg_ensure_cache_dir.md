# Ensure the Gutenberg cache directory exists

Checks for the existence of the cache directory and creates it if it is
missing.

## Usage

``` r
gutenberg_ensure_cache_dir()
```

## Value

The cache directory path (invisibly).
