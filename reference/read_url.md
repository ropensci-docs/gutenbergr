# Read a file from a URL

Quietly download, read, and delete file

## Usage

``` r
read_url(url)
```

## Arguments

- url:

  URL to a file

## Value

A character vector of lines of text or `NULL` if the file could not be
downloaded.
